server:
  port: 1234

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Kpidb
    username: root
    password: mamatha123
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
  spring:
  application:
    name: KpiService
