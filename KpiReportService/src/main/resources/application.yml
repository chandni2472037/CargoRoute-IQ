server:
  port: 2003

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cargoroute
    username: root
    password: root
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
      application:
       name: Report-service