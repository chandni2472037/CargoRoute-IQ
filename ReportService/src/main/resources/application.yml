server:
  port: 2003

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Reportdb
    username: root
    password: mamatha123
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
      application:
       name: Report-service